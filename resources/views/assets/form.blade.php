@extends('layouts.app')
@section('title', $asset->exists ? 'Editar activo' : 'Nuevo activo')
@section('heading', $asset->exists ? 'Editar activo' : 'Nuevo activo')
@section('content')
<div class="grid max-w-5xl gap-6 lg:grid-cols-2"><form class="panel p-6 space-y-4" method="POST" action="{{ $asset->exists ? route('assets.update',$asset) : route('assets.store') }}">@csrf @if($asset->exists) @method('PUT') @endif
<label class="field-label">Nombre<input class="field-input" name="name" value="{{ old('name',$asset->name) }}" required></label><label class="field-label">Código de activo<input class="field-input" name="asset_tag" value="{{ old('asset_tag',$asset->asset_tag) }}" required></label><label class="field-label">Número de serie<input class="field-input" name="serial_number" value="{{ old('serial_number',$asset->serial_number) }}"></label><label class="field-label">SKU<select class="field-input" name="sku_id"><option value="">Sin SKU</option>@foreach($skus as $sku)<option value="{{ $sku->id }}" @selected(old('sku_id',$asset->sku_id)===$sku->id)>{{ $sku->code }} · {{ $sku->product->name }}</option>@endforeach</select></label><label class="field-label">Tipo<select class="field-input" name="mobility"><option value="mobile" @selected(old('mobility',$asset->mobility)==='mobile')>Móvil</option><option value="static" @selected(old('mobility',$asset->mobility)==='static')>Estático</option></select></label><label class="field-label">Ubicación asignada<select class="field-input" name="location_id"><option value="">Sin asignar</option>@foreach($locations as $location)<option value="{{ $location->id }}" @selected(old('location_id',$asset->location_id)===$location->id)>{{ $location->name }}</option>@endforeach</select></label><label class="field-label">Estado<select class="field-input" name="status">@foreach(['active'=>'Activo','inactive'=>'Inactivo','maintenance'=>'Mantenimiento'] as $value=>$label)<option value="{{ $value }}" @selected(old('status',$asset->status ?: 'active')===$value)>{{ $label }}</option>@endforeach</select></label><button class="btn-primary">Guardar</button></form>
@if($asset->exists)<section class="panel p-6"><h2 class="font-semibold">Asignación de dispositivo</h2>@if($current=$asset->deviceAssignments->first())<div class="my-4 rounded-xl bg-emerald-50 p-4 text-sm"><strong>{{ $current->device->name }}</strong><br>{{ $current->tracking_strategy }}<form class="mt-2" method="POST" action="{{ route('asset-assignments.destroy',$current) }}">@csrf @method('DELETE')<button class="text-red-600">Finalizar asignación</button></form></div>@endif<form method="POST" action="{{ route('asset-assignments.store',$asset) }}" class="space-y-4">@csrf<label class="field-label">Dispositivo<select class="field-input" name="device_id">@foreach($devices as $device)<option value="{{ $device->id }}">{{ $device->name }} · {{ $device->type }}</option>@endforeach</select></label><label class="field-label">Estrategia<select class="field-input" name="tracking_strategy">@if($asset->mobility==='mobile')<option value="fixed_beacons_mobile_tracker">Tracker móvil detecta beacons fijos</option><option value="mobile_beacon_fixed_scanners">Beacon móvil detectado por scanners</option>@else<option value="assigned_static">Beacon asignado a activo estático</option>@endif</select></label><button class="btn-primary">Asignar dispositivo</button></form><form class="mt-8 border-t pt-4" method="POST" action="{{ route('assets.destroy',$asset) }}" onsubmit="return confirm('¿Archivar este activo?')">@csrf @method('DELETE')<button class="text-sm text-red-600">Archivar activo</button></form></section>@endif</div>
@endsection
